name: Publish

on:
  release:
    types:
      - published

jobs:
  publish:
    uses: ukasyah-dev/actions/.github/workflows/publish-npmjs.yml@main
    permissions:
      contents: read
    secrets: inherit
